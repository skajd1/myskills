interface:
  display_name: "SSH Runner"
  short_description: "Run bounded SSH commands with a local private key."
  default_prompt: "Use $ssh-runner to run a bounded command on one of my configured SSH hosts."
